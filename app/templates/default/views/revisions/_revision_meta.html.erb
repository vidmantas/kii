<span>As of <%= datetimestamp(@revision.created_at) %></span>

<%= link_to "Revert", confirm_destroy_page_revision_path(@page, @revision) if logged_in? %>
<%= link_to "Back to list", page_revisions_path(@page) %>

|

<%= link_to_unless_current "Plain", page_revision_path(@page, @revision) %>
<%= link_to_unless_current "Changes", page_revision_changes_path(@page, @revision) %>