<% page_title page_link(@page),
  link_to("Revisions", page_revisions_path(@page)),
  link_to("##{@revision.revision_number}",
  page_revision_path(@page, @revision)),
  "Changes" do %>
  <%= render :partial => "navigation" %>
<% end %>

<% render :partial => "pages/navigation" %>

<div class="diff pre_block"><%= Kii::Diff::HTML.diff(@previous_revision_body, @revision.body) %></div>