<% content_for(:sidebar) do %>
  <ul id="tabs" class="simple">
    <%= tab "All revisions", revisions_activities_path %>
    <%= tab "Others revisions", others_revisions_activities_path(:id => current_user.id) if logged_in? %>
    <%= tab "Discussions", discussions_activities_path %>
  </ul>
<% end %>